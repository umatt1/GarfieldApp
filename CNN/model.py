import torch
import torch.nn

class model(torch.nn.Module):
    def __init__(self, n_input_features):
        super(model, self).__init__()

        # use relu for hidden layers
        # use sigmoid for last layer activation function
        return 

    def forward(self, X):
        return 